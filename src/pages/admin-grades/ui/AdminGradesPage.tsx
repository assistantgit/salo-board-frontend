import { AdminPageLayout } from '@widgets/admin-page-layout';
import type React from 'react';

export const AdminGradesPage: React.FC = () => {
  return (
    <AdminPageLayout title='Оцінки' subtitle='Керування оцінками та результатами'>
      {/* Content will be added here */}
    </AdminPageLayout>
  );
};
