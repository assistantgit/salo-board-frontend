events {
    worker_connections 1024;
}

http {
    map $proxy_host $upstream_host {
        default $proxy_host;
    }

    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }
}
